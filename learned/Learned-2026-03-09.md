# Learned - 2026-03-09

## 今日完成
- 完成「每日 AI 新聞摘要」任務，成功輸出並存檔：`/Users/yenhaohuang/Desktop/daily/ainews/AI-News-2026-03-09.md`。
- 在外部搜尋能力受限時，仍以替代流程完成 6 則新聞整理與 3 點觀察。
- 完成一次「每日學習回顧」彙整與落檔（本檔案）。

## 今日學到
- **工具降級策略很重要**：`web_search` 缺少 API key 時，應快速切換到可行來源（例如 Google News 頁面與 RSS）。
- **命令環境需先驗證**：`python` 不存在、`feedparser` 套件缺失，改用 `python3 + curl/perl/XML` 的無相依方案可提高穩定性。
- **先保交付，再優化品質**：在限制條件下優先完成可交付成果，避免任務卡住。

## 問題與改進
- 問題：`web_search` 無法使用（`missing_brave_api_key`）。
  - 改進：補齊 Brave API key，恢復標準搜尋流程。
- 問題：腳本依賴 `feedparser`，環境未安裝導致失敗。
  - 改進：建立「零第三方依賴」預設備援腳本（純 `curl + XML`）。
- 問題：部分來源為 Google News 聚合連結，可讀性與可追溯性較弱。
  - 改進：後續嘗試補抓原始新聞網址，提升來源品質。

## 明日重點
- 優先修復資料來源能力（API key / 搜尋配置），降低手動繞道成本。
- 將「失敗即切換」流程標準化：先測工具可用性，再決定抓取管道。
- 持續優化摘要品質：在可交付前提下，提升來源多樣性與連結可讀性。
