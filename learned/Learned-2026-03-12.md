# Learned-2026-03-12

## 今日完成
- 完成台灣財經新聞摘要檔案輸出：`/Users/yenhaohuang/Desktop/daily/financenews/Finance-News-2026-03-12.md`。
- 完成資料夾自動建立流程（若目錄不存在則先建立再寫檔）。
- 完成今日學習回顧彙整與輸出（本檔）。

## 今日學到
- 在 `web_search` 無法使用（缺少 Brave API key）時，可改以 `python3 + Google News RSS` 作為替代資料來源，維持任務可完成性。
- 工具環境需先確認執行命令可用性：`python` 不存在時，改用 `python3`。
- 工作流程上，先建立目錄再寫檔能降低失敗率，適合做為固定模板。
- 任務結果回報格式應包含「完成狀態 + 檔案實際路徑」，方便後續追蹤。

## 問題與改進
- 問題：`web_search` 因 API key 缺失而失敗，造成原始方案中斷。
  - 改進：預先檢查關鍵外部依賴（搜尋 API、權限、環境變數），並準備 fallback 清單（RSS / 指定來源）。
- 問題：初次嘗試使用 `python` 指令失敗。
  - 改進：在腳本任務前先做一次 `python3 --version` 或工具可用性檢查。
- 問題：主會話可追蹤紀錄偏少，學習回顧素材有限。
  - 改進：將每日重要決策與踩坑即時寫入 `memory/YYYY-MM-DD.md`，晚間回顧可更完整。

## 明日重點
- 優先確認並補齊 `web_search` 所需 API 設定，避免再次改道。
- 建立「任務前檢查清單」：工具可用性、目錄存在、輸出路徑、備援方案。
- 每完成一項排程任務，補一條「做法/錯誤/修正」到當日 memory，累積可複用 SOP。
- 延續今日做法，維持輸出品質：條列清楚、路徑明確、可直接追蹤。